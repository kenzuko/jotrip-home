# V3 data migration

This directory is the source layer for Open Phu Quoc V3.

During migration, existing production JSON files remain in their original locations. V3 paths are introduced in parallel so the current site does not break before adapters and views are switched over.
