const CONTENT="../data/content.json";const $=s=>document.querySelector(s);async function load(){const r=await fetch(CONTENT+"?t="+Date.now(),{cache:"no-store"});if(!r.ok)throw new Error(r.status);return r.json()}function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}function card(s){return '<a class="story-card" href="article.html?id='+encodeURIComponent(s.id)+'"><img src="'+esc(s.image)+'" alt="'+esc(s.title)+'" onerror="this.style.opacity=.18"><div class="story-copy"><span>'+esc(s.category)+'</span><h2>'+esc(s.title)+'</h2><p>'+esc(s.dek)+'</p></div></a>'}function renderArticle(data){const id=new URLSearchParams(location.search).get("id");const s=data.stories.find(x=>x.id===id)||data.stories[0];document.title=s.title+" - Open Phu Quoc";const root=$("#articleRoot");root.innerHTML='<article><header class="article-hero"><img src="'+esc(s.image)+'" alt="'+esc(s.title)+'" onerror="this.style.opacity=.18"><div class="wrap"><p class="eyebrow">'+esc(s.category)+'</p><h1>'+esc(s.title)+'</h1><p class="dek">'+esc(s.dek)+'</p><div class="article-meta">'+s.read_minutes+' PHÚT ĐỌC · OPEN PHU QUOC</div></div></header><div class="article-body"><p class="intro">'+esc(s.intro)+'</p>'+s.sections.map((x,i)=>'<section><h2>'+esc(x.heading)+'</h2><p>'+esc(x.body)+'</p></section>'+(i===1&&s.sections[2]?'<aside class="story-pause"><span>ĐỌC CHẬM MỘT CHÚT</span><strong>'+esc(s.sections[2].heading)+'</strong><p>'+esc(s.sections[2].body).slice(0,180)+(String(s.sections[2].body).length>180?'...':'')+'</p></aside>':'')).join("")+'<div class="note">Bản demo editorial. Những thông tin vận hành như giờ mở cửa, giá, trạng thái show và điều kiện thời tiết sẽ được lấy từ lớp dữ liệu riêng thay vì đóng cứng trong bài.</div><div class="sources"><h3>Nguồn tham khảo</h3>'+s.sources.map(x=>'<a href="'+esc(x.url)+'" target="_blank" rel="noopener">'+esc(x.label)+' →</a>').join("")+'</div></div></article>';const i=data.stories.indexOf(s),n=data.stories[(i+1)%data.stories.length];$("#nextStory").innerHTML='<a href="article.html?id='+encodeURIComponent(n.id)+'"><span>Đọc tiếp: '+esc(n.title)+'</span><span>→</span></a>'}load().then(data=>{const grid=$("#storyGrid");if(grid)grid.innerHTML=data.stories.map(card).join("");if($("#articleRoot"))renderArticle(data)}).catch(()=>{const root=$("#articleRoot")||$("#storyGrid");if(root)root.innerHTML='<div class="wrap"><p>Chưa tải được nội dung.</p></div>'});